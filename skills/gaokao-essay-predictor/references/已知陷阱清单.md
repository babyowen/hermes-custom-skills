# 高考作文预测 — 已知陷阱清单

> 自动整理于 2026-05-30。共 26 条，分为 6 大类。

## 分类统计

| 类别 | 数量 | 说明 |
|:-----|:-----|:------|
| A. 搜索/数据获取类 | 3 | Exa 额度、web_extract 无回退、curl WAF |
| B. 飞书/文件操作类 | 9 | --mode、缓存、patch 转义、目录同步 |
| C. cron 配置类 | 2 | prompt 覆盖、deliver 错误 |
| D. 命题/预测逻辑类 | 8 | 冲刺期视角、排除表格、政治表态 |
| E. 维护类 | 2 | 偏地域素材、标注日期 |
| F. 非阻塞警告 | 2 | v1 警告、lark-table 格式丢失 |

## 完整清单

### A. 搜索/数据获取类

| # | ❌ 别这么干 | 后果 | ✅ 正确做法 |
|:-:|:-----------|:----|:------------|
| 1 | Exa API 额度用完了还继续调 web_search | 一直报 402，浪费时间 | 用 SerpAPI 回退：`python3 ~/.hermes/skills/serpapi-search/scripts/search.py "关键词" --country cn --lang zh-CN --limit 5 --json` |
| 2 | web_search 失败后，web_extract 还想硬用 | web_extract 和 web_search 共享 Exa 额度，也报 402 | web_extract 无 SerpAPI 回退；改用 curl 或 browser_navigate |
| 3 | curl 硬爬中文新闻站（腾讯新闻/搜狐等） | 被 WAF 反爬拦截，返回空页 | 用 browser_navigate（完整浏览器渲染），或用搜索结果摘要/snippet 代替 |

### B. 飞书/文件操作类

| # | ❌ 别这么干 | 后果 | ✅ 正确做法 |
|:-:|:-----------|:----|:------------|
| 4 | `lark-cli docs +update` 不加 `--mode` | 静默失败，报 `--mode is required` | 必须加 `--mode overwrite`（全量覆盖）或对应模式 |
| 5 | 用 `patch` 修改飞书导出文件的引号包裹字段 | 引号转义逐层恶化，不可逆 | 用 `write_file` 全文覆盖；推送飞书用 `cat <file> | lark-cli docs +update --doc <token> --markdown - --mode overwrite` |
| 6 | 更新飞书文档后不同步本地缓存 | 下次运行读到的是过时版本 | 更新后马上：`lark-cli docs +fetch --doc <token> --format pretty > references/<文件名>` |
| 7 | 首次运行时没有本地缓存文件就卡住 | references/ 下大部分文件可能不存在 | 直接从飞书拉取三个核心文档即可运行；教育部命题方向不存在时用 web_search 搜索 |
| 8 | 热点素材库中插入新热点后不重新编号 | 后面条目编号乱掉 | 插入后**必须重新编号**所有后续条目（8→9, 9→10...） |
| 9 | 向作文素材库追加事件时不管表格格式 | 表格格式断裂，上一事件的结尾格式丢失 | 每追加一个 Event 后检查上一个 Event 的结尾格式是否正确 |
| 10 | 用 `patch` 时 old_string 不唯一 | 报错 `Found N matches` | 用比引号上下文更多的相邻行（3-5行文本+标签）做 old_string；或改用 `write_file` 全文覆盖 |
| 11 | cron prompt 写了一大段自有的业务逻辑 | cron prompt 覆盖 skill SKILL.md 的内容，skill 更新后 cron 仍执行旧逻辑 | cron prompt 只写"按 skill 执行"+交付地址；所有流程放 SKILL.md |
| 12 | 更新飞书后不同步另一个目录的缓存 | 双目录（skill 目录 + hermes-agent 工作副本）缓存不一致 | 更新后用 `cp` 在两个目录间同步三个核心缓存文件 |

### C. cron 配置类

| # | ❌ 别这么干 | 后果 | ✅ 正确做法 |
|:-:|:-----------|:----|:------------|
| 13 | deliver 设成 `origin` | 投递到私聊而非群聊 | 必须设成 `feishu:oc_5d97d211e77becee79ff4241a4b10568` |
| 14 | cron prompt 中包含与 skill 冲突的判断逻辑 | 同时有两套判断规则，互相覆盖 | 考前检查 cron prompt 是否已简化，确保无多余逻辑残留 |

### D. 命题/预测逻辑类

| # | ❌ 别这么干 | 后果 | ✅ 正确做法 |
|:-:|:-----------|:----|:------------|
| 15 | 冲刺期还追当周最新事件 | 5月23日后命题已定稿，追新无意义 | 切换为年度全景扫描：回看 2025.06-2026.06 全年，像命题组一样思考 |
| 16 | 在报告中制作"方向排除等级"表格 | 标记方向为「排除」「预警」「警惕」——模拟考热点不是排除信号 | 只列观察到的主流方向，不做排除判断 |
| 17 | 写政治表态（歌颂/口号/直接引用外交辞令） | 高考作文不考政治表态 | 剥去政治外衣，提取哲学思辨内核 |
| 18 | 纯科技叙事，无人文浸润 | 2026命题方向是"科技+人文"融合 | 科技方向必须配套人文教育元素 |
| 19 | 用偏地域素材 | 对农村/偏远地区考生不公平 | 所有方向确保城乡考生都能写 |
| 20 | 出模拟题时不检查是否与近3年真题雷同 | 可能重复已考过的主题 | 先读历年作文题汇编，确认2023故事、2024AI问题、2025民族魂没重复 |
| 21 | 出题后不顾自检清单 | 容易漏公平性/思辨性/反宿构检查 | 每次出题后过模拟题自检清单 |
| 22 | 不关注整数纪念年 | 高考命题经常以整数周年为思辨切入点 | 搜索 20/25/30/35/40/45/55/60/70/80/90/100 周年事件，提炼思辨角度 |

### E. 维护类

| # | ❌ 别这么干 | 后果 | ✅ 正确做法 |
|:-:|:-----------|:----|:------------|
| 23 | 更新素材库内容时不标日期 | 用户不知道内容新鲜程度 | 每次更新在对应条目末尾或文档顶部标注`（更新于YYYY-MM-DD）` |
| 24 | 认为所有 reference 文件都必须存在才能运行 | 大部分文件是可选的上下文参考 | 直接跑 cron，缺失的文件下次运行再逐步填充 |

### F. 非阻塞警告

| # | ❌ 别这么干 | 后果 | ✅ 正确做法 |
|:-:|:-----------|:----|:------------|
| 25 | 看到 `[deprecated] using v1 API` 就以为功能受阻 | 这是 lark-doc skill 版本问题，功能不受影响 | 正常运行命令即可；检查 lark-doc skill 是否有 v2 版本 |
| 26 | 依赖 `lark-table` HTML 标签做格式 | lark-cli 1.0.44 不再支持私有标签，表格格式丢失 | 内容完整保留，仅视觉效果变差；需关注 lark-doc 后续版本 |
